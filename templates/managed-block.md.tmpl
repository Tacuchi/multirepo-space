<!-- MULTIREPO_SPACE_MANAGED:START -->
## Multirepo Space Managed Init

This repo is linked to workspace `{{workspace_name}}` at `{{workspace_path}}`.
Repo alias in workspace: `{{alias}}`.
Primary specialist: `repo-{{alias}}`.

Tool compatibility:
- Codex: use workspace `AGENTS.md` and `.agents/repo-{{alias}}.md`.
- Claude Code: use workspace `CLAUDE.md` and `.claude/agents/repo-{{alias}}.md`.

Suggested verification commands:
{{verify_cmds_list}}
<!-- MULTIREPO_SPACE_MANAGED:END -->
