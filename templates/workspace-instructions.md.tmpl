# Orquestador Multi-Repo

Workspace central que gestiona {{N}} {{repos_word}} externos. No contiene codigo propio; orquesta, documenta y consolida.

## Repos

| Alias | Stack | Ruta (symlink en `repos/`) |
|-------|-------|----------------------------|
{{repos_table}}

## Agentes

| Agente | Rol | Scope |
|--------|-----|-------|
{{agents_table}}

- Invocar especialistas via `/agents` o `Task`.
- Agentes globales opcionales (arquitectura, estilo, etc.) pueden ser invocados por coordinador y especialistas.

## Reglas

- Documentacion y artefactos: `./docs/<tema>/`. Scripts: `./scripts/`.
- **No crear** documentacion dentro de repos externos — solo codigo y config.
- Planificar antes de tocar codigo. Cambios minimos y testeables.
- Reporte obligatorio: archivos tocados, motivo, comandos de verificacion.

## Compatibilidad

- **Claude Code**: `CLAUDE.md`, `.claude/agents/`, `.claude/settings.json`
- **Codex/Gemini/Cursor**: `AGENTS.md`, `.agents/`

Fecha: {{today}}
