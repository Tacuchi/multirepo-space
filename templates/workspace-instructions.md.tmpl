# Orquestador Multi-Repo

Workspace central — {{N}} {{repos_word}} externos. Orquesta, documenta y consolida.

## Repos

| Alias | Stack | Ruta (symlink en `repos/`) |
|-------|-------|----------------------------|
{{repos_table}}

## Agentes

| Agente | Rol | Scope |
|--------|-----|-------|
{{agents_table}}

→ Invocar especialistas via `/agents` o `Task`. Agentes globales disponibles para coordinador y especialistas.

## Reglas

→ Docs/artefactos: `./docs/<tema>/`. Scripts: `./scripts/`.
→ **No crear** docs dentro de repos externos.
→ Planificar → cambios minimos → reportar archivos tocados, motivo, verificacion.

## Compatibilidad

**P0 (principal)**
Claude Code: `CLAUDE.md`, `.claude/agents/`, `.claude/settings.json` | Codex: `AGENTS.md`, `.agents/`, `.agents/skills/`

**P1 (extendido)**
Gemini: `.gemini/agents/` | OpenCode: `.opencode.json` | Crush: `.crush.json`

**P2 (orquestacion cloud)**
Warp Oz: `.agents/skills/` + `docs/warp-oz/environment-example.md`

Fecha: {{today}}
